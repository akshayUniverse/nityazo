import { StyleSheet } from "react-native";

export const CategoryCardStyles = StyleSheet.create({
  container: {
    width: "47%",
    marginBottom: 20,
    alignItems: "center",
  },

  image: {
    width: "100%",
    height: 125,
    borderRadius: 20,
  },

  title: {
    marginTop: 8,
    fontSize: 18,
    fontWeight: "500",
    color: "#222",
  },
});
