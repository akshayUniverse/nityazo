import { StyleSheet } from "react-native";

export const CategoryCardStyles = StyleSheet.create({
  container: {
    width: "47%",
    marginBottom: 18,
    alignItems: "center",
  },

  image: {
    width: "100%",
    height: 105,
    borderRadius: 14,
  },

  title: {
    marginTop: 8,
    fontSize: 18,
    fontWeight: "500",
    color: "#222",
  },
});
